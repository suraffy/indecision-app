import Modal from "react-modal";

const OptionModal = (props) => {
  return (
    <Modal>
      <h3>Selected Option</h3>
      <p>selectedOption</p>
      <button>Okay</button>
    </Modal>
  );
};

export default OptionModal;
